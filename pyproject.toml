[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tech-watch-scheduler-factory"
version = "0.3.1"
description = "Generate repository-specific research instructions and public watch sites"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["tech_watch"]
