[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tech-watch-scheduler-factory"
version = "0.2.0"
description = "Generate repository-specific research instructions, not live timers"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["tech_watch"]
